# CPU 100% 问题怎么排查？

## 核心概念

CPU 100% 表示机器或容器的 CPU 资源被持续打满，可能来自业务线程忙循环、频繁 GC、锁竞争、正则灾难回溯、加解密/压缩计算、流量突增等。

排查目标是找到 **哪个进程、哪个线程、哪段代码** 消耗 CPU。

## 面试官想考什么

- 是否掌握线上 CPU 排查链路；
- 是否能把 Linux 线程 ID 和 Java 线程栈对应起来；
- 是否知道 CPU 高可能和 GC、死循环、锁竞争有关；
- 是否有止血、定位、复盘意识。

## 标准回答

> 我会先用 `top` 找到 CPU 高的 Java 进程，再用 `top -Hp <pid>` 找到进程内 CPU 高的线程，把线程 ID 转成 16 进制后，用 `jstack <pid>` 查看对应线程栈，定位具体业务方法。如果同时伴随频繁 GC，会结合 `jstat`、GC 日志和堆 dump 判断是否是内存压力导致。线上处理时先考虑扩容、限流、重启或摘流量止血，再做根因分析。

## 排查步骤

```bash
# 1. 找到 CPU 高的进程
top

# 2. 查看进程内线程 CPU
top -Hp <pid>

# 3. 将线程 ID 转 16 进制
printf '%x\n' <tid>

# 4. 查看 Java 线程栈
jstack <pid> | grep -A 30 '<nid十六进制>'

# 5. 查看 GC 情况
jstat -gcutil <pid> 1000 10
```

## 深挖追问

### 如果 jstack 看不到明显业务栈怎么办？

可以多抓几次线程栈对比，确认线程是否一直停留在同一位置；同时检查 GC、JNI、系统调用、容器 CPU 限额、日志刷盘等因素。

### 线上能直接 kill 进程吗？

不建议第一时间粗暴 kill。应先评估是否有多副本、是否能摘除流量、是否会影响交易。必要时保留现场信息后再重启。

## 易错点/总结

- 只看进程 CPU 不够，要定位到线程和代码；
- 线程 ID 需要转成 16 进制匹配 `nid`；
- CPU 高不一定是业务死循环，也可能是频繁 GC；
- 排障前尽量保留线程栈、GC 日志和关键监控截图。

<!-- interview-renovation:2026-06-24 -->

## 面试复习强化

### 核心概念

从面试角度看，**CPU 100% 问题怎么排查？** 可以放在“系统设计与排障”这一类知识中理解。复习时不要只背定义，要能说清：它解决什么问题、依赖哪些前提、正常流程是什么、异常情况下系统会怎样退化或恢复。

### 面试官想考什么

- 是否理解概念背后的设计目标，而不是只记住名词；
- 是否能把机制和真实工程场景联系起来；
- 是否能分析边界条件、失败场景、性能与安全取舍；
- 是否能给出可落地的排查、实现或优化步骤。

### 标准回答

> 按“现象确认→指标定位→根因分析→临时止血→长期治理”回答。 追问会覆盖流量、线程/连接池、缓存、数据库、MQ、下游依赖、机器资源和发布变更。 对于“CPU 100% 问题怎么排查？”，回答时建议先给一句话定义，再按“工作流程/关键机制 → 典型场景 → 风险与优化”展开，最后补充一两个线上实践点。

### 深挖追问

- 如果该机制失效，会出现什么现象？如何定位是配置、代码、资源还是外部依赖导致？
- 它和相邻概念有什么区别？例如语义、适用场景、性能成本、可靠性保证分别是什么？
- 在高并发、网络抖动、服务重启、数据不一致或权限受限时，需要补充哪些保护措施？
- 有哪些指标可以证明方案有效？例如延迟、吞吐、错误率、资源使用率、重试次数或业务成功率。

### 示例 / 实战场景

- 设计方案时：先明确业务目标和约束，再选择对应机制，不要为了使用某个技术而引入复杂度。
- 排查问题时：先确认现象和影响面，再查看日志、监控、配置、版本变更和上下游依赖，最后小步验证修复。
- 复盘沉淀时：补充自动化测试、容量评估、告警阈值、降级预案和文档，避免同类问题再次发生。

### 本题高频补充

- CPU 排查要落到 top -H、jstack、火焰图、GC 日志和热点代码，区分用户态、内核态和 IO wait。

### 易错点 / 总结

- 只背结论、不讲原因，是面试扣分点；要主动解释“为什么这样设计”。
- 只讲正常路径、不讲异常路径，会显得缺少生产经验；至少补充超时、重试、降级、回滚或兜底。
- 不要把理论保证无限放大，工程实现通常还受网络、资源、配置、版本和业务语义约束。
- 总结一句：给结论前先给证据，实战动作要包括监控、日志、压测复现、灰度回滚、限流降级和容量评估。


# 数据类型-Stream

数据类型-Stream
Redis Stream 是 Redis5.0 版本新增加的数据类型，Redis 专门为消息队列设计的数据类型。

历史版本的消息队列存在缺陷：
发布订阅模式不能持久化，也就无法可靠保存消息，并且对于离线重连的客户端不能读取历史消息的缺陷；
List实现消息队列的方式不能重复消费，一个消息消费完就会被删除，而且生产者需要自行实现全局唯一的消息ID；

Stream类型完美实现消息队列，支持消息持久化、支持自动生成全局唯一ID、支持ack确认消息模式、支持消费组模式等，让消息队列更加稳定可靠。

# 底层数据结构-双向链表

底层数据结构-双向链表
链表结构 list 组成：
head：指向链表头节点 listNode 的指针；
tail：指向链表尾节点 listNode 的指针；
len：链表节点数量；
free：节点释放函数；
match：节点比较函数；
dup：节点复制函数；

链表节点结构 listNode 组成：
prev：前置节点指针；
next：后置节点指针；
value：当前节点的值对象指针；

链表的优势：
listNode 链表节点的结构里带有pre和next指针，获取某个节点的前置节点或后置节点的时间复杂度O(1)，而且两个指针允许指向NULL，所以链表是无环链表；
list 链表结构提供了 head 和 tail 指针，获取链表的表头节点和表尾节点的时间复杂度O(1)；
list 链表结构提供了链表节点数量len，所以获取链表中的节点数量时间复杂度O(1)；
listNode 链表节点结构使用 void* 指针保存节点值，并可以通过 list 结构的 dup、free、mach 函数指针为节点设置该节点类型特定的函数，因此链表支持保存各种不同类型的值；

链表的缺陷：
链表每个节点之间的内存不连续，无法很好利用CPU的缓存；
保存一个链表节点的值都需要一个链表节点结构头的分配，内存开销较大；
因此 Redis3.0 的 List 对象在数据量比较少的情况下，采用压缩列表作为底层数据结构的实现，它的优势是节省内存空间，并且内存紧凑型的数据结构。

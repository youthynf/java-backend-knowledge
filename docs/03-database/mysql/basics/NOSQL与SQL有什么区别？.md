# NOSQL与SQL有什么区别？

## 核心概念

NOSQL与SQL有什么区别？
主要区别：
SQL数据库：关系型数据库存储结构化数据，这些数据逻辑上以行列二维表的形式存在，每一列代表数据的一种数据，每一行代表一个数据实体；
NOSQL数据库：非关系型数据库提供了不同于二维表的存储方式，其存储方式可以是JSON文档、哈希表或者其他方式；

实际应用：
SQL数据库：指关系型数据库，主要代表：SQL Server、Oracle、MySQL、PostgreSQL；
NOSQL数据库：指非关系型数据库，主要代表：MongoDB、Redis；

场景选择：
SQL数据库：支持ACID即原子性，一致性，隔离性和持续性；
NoSQL数据库：采用的是更宽松的模型BASE，即基本可用，软状态和最终一致性。

扩展对比：
SQL数据库：数据之间存在关联性，水平扩展较难，需要解决跨服务器JOIN、分布式事务等问题；
NOSQL数据库：数据之间无关联关系，这样容易扩展，比如Redis自带主从复制模式、哨兵模式、切片集群模式；

## 面试复习要点

面试中回答“NOSQL与SQL有什么区别”时，不要只给结论，建议按 **定位 → 原理 → 使用边界 → 排查方式** 展开：

1. **先讲定位**：说明它解决的核心问题，以及在整个技术栈中处于哪一层。
2. **再讲原理**：把关键流程、核心数据结构或关键组件讲清楚，避免只背名词。
3. **补充边界**：说明它适合什么场景，不适合什么场景，以及常见误用。
4. **结合排查**：如果线上出现性能、稳定性或一致性问题，要能说出观测指标、日志线索和排查顺序。

## 标准回答思路

可以这样组织答案：**NOSQL与SQL有什么区别 的核心价值是解决特定场景下的工程问题。实际使用时，需要理解它的工作机制、成本和限制，不能只看 API 或表面现象。在线上落地时，还要结合监控、日志和压测结果判断是否真的适合当前业务。**

## 常见追问

- 它和相近方案的区别是什么？
- 如果数据量、并发量或链路复杂度上来，会先暴露什么问题？
- 线上出现异常时，你会先看哪些指标，如何缩小范围？

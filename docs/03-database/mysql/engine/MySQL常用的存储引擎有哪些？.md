# MySQL常用的存储引擎有哪些？

MySQL常用的存储引擎有哪些？
InnoDB存储引擎：
InnoDB是MySQL的默认存储引擎，具有ACID事务支持、行级锁、外检约束等特性，适用于高并发读写操作，支持比较好的数据完整性和并发控制；

MyISAM存储引擎：
MyISAM是MySQL的另一种比较场景的存储引擎，具有较低的存储空间和内存消耗，适用于大量读操作的场景，但是MyISAM不支持事务、行级锁和外键约束，支持表级锁、全文索引。在并发写入和数据完整性方面有一定限制；

Memory存储引擎：
Memory将数据存储在内存，适用于对性能要求较高的读操作，但是在服务器重启或者崩溃时数据会丢失，它不支持事务、行级锁和外键约束，支持哈希索引。

CSV存储引擎：
CSV是以逗号分隔值格式存储数据，数据文件直观易懂，便于查看和编辑；简单的读写操作，不支持索引。

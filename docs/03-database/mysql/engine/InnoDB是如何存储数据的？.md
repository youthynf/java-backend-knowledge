# InnoDB是如何存储数据的？

InnoDB是如何存储数据的？
InnoDB存储单位与读写单位
MySQL 支持多种存储引擎，不同的存储引擎，存储数据的方式也是不同的，我们最常使用的是 InnoDB 存储引擎，所以以InnoDB进行分析。数据库的数据记录是按「行」来存储的，但是数据库的读取并不是以「行」为单位，否则一次读取（也就是一次 I/O 操作）只能处理一行数据，效率会非常低。因此，InnoDB 的数据是按「数据页」为单位来读写的，也就是说，当需要读一条记录的时候，并不是将这个记录本身从磁盘读出来，而是以页为单位，将其整体读入内存。

数据页的基本组成
数据库的 I/O 操作的最小单位是页，InnoDB 数据页的默认大小是 16KB，意味着数据库每次读写都是以 16KB 为单位的，一次最少从磁盘中读取 16K 的内容到内存中，一次最少把内存中的 16K 内容刷新到磁盘中。数据页由7部分组成：文件头、页头、最小最大记录、用户记录、空闲空间、页目录、页尾：
文件头：表示页的信息，包含两个指针，分别指向上一个和下一个数据页，连接起来形成双向链表，从而实现不需要物理上连续，而是逻辑连续；
页头：表示页的状态信息，是否为脏等；
最小和最大值：两个虚拟的伪记录，分别表示页中最小记录和最大记录；
用户记录：存储行记录内容，数据页中的记录按照「主键」顺序组成单向链表，方便插入和删除，但是检索效率不高，需要遍历链表上的所有节点才能完成检索。
空闲空间：页中还没有被使用的空间；
页目录：存储用户记录的相对位置，对记录起到索引作用。页目录的创建时，将所有的记录分为几个组，这些记录包含最小和最大记录，但是不包括已删除的记录；每个组中最后一条记录就是组内最大的记录，并且最后一条记录的头信息中都会存储该组一共有多少条记录作为n_owned字段；而页目录是用来存储每组最后一条记录的地址偏移量，这些偏移量会按顺序存储起来，每组的地址偏移量也被成为槽（slot），每个槽相当于指针指向组中最后一条记录。
InnoDB对每个分组的记录条数都是有规定的：
第一个分组中的记录只有1条记录，即最小记录；
最后一个分组中的记录数范围只能是1-8条之间；
剩下的分组记录条数范围只能是4-8之间；
文件尾：校验页的完整性。

# TCP四次挥手如果服务端的FIN报文比数据报文先到会发生什么？
TCP四次挥手过程中，服务端发送最后的数据报文和FIN报文，其中数据报文发生了网络阻塞，导致最终FIN报文更早到达客户端，此时客户端会发生什么？

客户端在FIN_WAIT_2状态收到这个乱序的FIN报文后，会将报文放到一个乱序队列，而不是直接进入TIME_WAIT状态。后续收到被网络延迟的数据报文后，会判断这个乱序队列中有没有数据，然后会检测乱序队列中是否有与当前序列号保持顺序的报文，然后看这个报文是否有FIN标志，如果发现有FIN标志，则进入TIME_WAIT状态。

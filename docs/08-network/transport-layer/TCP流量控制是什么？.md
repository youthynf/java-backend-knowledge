# TCP流量控制是什么？
什么是流量控制？
发送方不能无脑的发送数据给对方，要考虑接收方的处理能力，如果一直发送，而对方处理不过来，那么会导致触发「超时重传」机制，从而导致网络流量无端浪费。为了解决这种现象发生，TCP提供了一种机制可以让「发送方」根据「接收方」的实际接收能力控制发送的数据量，这就是所谓的「流量控制」。

操作系统缓冲区与滑动窗口关系
本质上，发送窗口和接收窗口中所存放的字节数，都是放在操作系统缓冲区中的，而实际操作系统的缓冲区会被操作系统调整。
正常情况下，如果缓冲区中接收到的数据没有被应用程序读取，那么接收窗口会相应减少这部分数据的字节数大小，并通知发送方，从而调整发送量。
如果服务端繁忙，操作系统可能会直接减少接收缓存区大小，减少后接收窗口会相应减少对应字节数并告知发送端，一旦这个告知消息延迟，发送端按照原有的窗口大小发送的数据包先到达，服务端接收到这个数据包后发现数据大小超过了可接收窗口的大小，于是就把数据包丢失了。因此为了防止这种情况发生，TCP规定是不允许同时减少缓存又收缩窗口的，而是采用先收缩窗口，过段时间再减少缓存，这样可以避免丢包的情况。

窗口关闭
TCP通过让接收方指明希望从发送方接收的数据大小来进行流量控制。如果窗口大小为0时，就会阻止发送方给接收方传递数据，直到窗口变为非0为止，这就是窗口关闭。

窗口关闭潜在问题：当发生窗口关闭时，接收方处理完数据后，会向发送方通告一个窗口非0的ACK报文，如果这个ACK报文在网络中丢失，这就会导致发送方一直等待接收方的非0窗口通知，接收方也一直等待发送方的数据，如果不采取措施，将会导致互相等待的死锁现象。
为了解决这个问题，TCP为每个连接设有一个持续定时器，只要TCP连接一方接收到对方的0窗口通知，就启动持续计时器。如果持续计时器超时，就会发送窗口探测（Window probe）报文，而对方在确认这个探测报文时，给出自己现在的接收窗口大小。窗口探测的次数一般为3次，每次大约30-60秒，如果3次过后接收窗口还是0的话，有的TCP实现就会发送RST报文来中断连接。

糊涂窗口综合症
如果接收方太忙了，来不及取走接收窗口里的数据，那么就会导致发送方的窗口越来越小。到最后，如果接收方腾出几个字节并告诉发送方现在有几个字节的窗口，而发送方会义无反顾地发送这几个字节，这就是「窗口糊涂综合症」。我们TCP+IP的头部信息有40字节，为了传输那么几个字节数据，要搭上这么大的开销，这太不经济了。

如何解决？
接收方通常策略：如果「窗口大小」小于min(MSS，缓存个空间/2)，也就是小于MSS与1/2缓存区大小的最小值时，就会向发送方通告窗口为0，也就阻止了发送方再发来数据。等接收方处理了一些数据后，窗口大小大于等于MSS或者接收方缓存空间有一半以上可以使用，就把窗口打开让发送方发送数据过来。
发送方通常策略：使用Nagle算法，核心思路是延时处理，只有满足「窗口大小≥MSS且数据大小≥MSS」时，或者「收到之前发送数据的ACK回包」时，如果这两个条件都不满足，发送方一直囤数据，直到满足这两个发送条件的任意一个为止。Nagle算法默认打开。
如果接收方不能满足不通告小窗口给发送方，即使发送方开启了Nagle算法，也无法避免糊涂窗口综合症，因为如果对端ACK回复很快，那么发送方满足Nagle算法的第二个条件，Nagle算法就不会拼接太多的数据包，这种情况下依然存在小数据包的传输，网络总体利用率依然很低。所以，接收方得满足不通告小窗口给发送方，且发送方开起来Nagle算法，才能避免糊涂窗口综合症。

# TCP滑动窗口是什么？
什么是窗口？
我们知道TCP每发送一个数据，都需要进行一次确认应答，如果我们每次都需要收到上一个报文的应答后才发送下一个报文，这样的传输方式很低效，RTT越长，则通信效率越低，因此引入「窗口」的概念。
有了窗口，就可以指定窗口大小，所谓的窗口大小是指无需等待确认应答，而可以继续发送数据的最大值。窗口实际上是在操作系统上开辟了一个缓存空间，发送方主机在等到确认应答返回之前，必须在缓冲区中保留已经发送的数据，如果按期收到确认应答，此数据就可以从缓冲区中清除。

累计确认/累计应答
如果发送方连续发送多个TCP段，并且中途若有ACK丢失，可以通过「下一个确认应答」进行确认，如ACK600丢失，只需要发送方收到ACK700确认应答，意味着700之前的所有数据接收方都已经收到，这个模式就叫「累计确认」或「累计应答」。

窗口大小哪一方决定？
TCP头部里有一个字段叫window，也就是窗口大小。这个字段是接收端告诉发送端自己还有多少缓冲区可以接收数据。于是发送端可以根据这个接收端的处理能力来发送数据，而不会导致接收端处理不过来。所以，通常窗口的大小是有接收方的窗口大小来决定的，发送方发送的数据大小不能超过接口方的窗口大小，否则接收方就无法正常接收到数据。

发送方滑动窗口
发送方缓存的数据根据处理的情况可以分为四部分，分别是;
已发送并收到ACK确认的数据
已发送但未收到ACK确认的数据（发送窗口）
未发送但总大小在接收方处理范围内的数据（可用窗口）
未发送但总大小超出接收方处理范围的数据
如果发送方将接收方可处理范围内的数据全部发送，则可用窗口大小为0，表明可用窗口耗尽，在没有收到ACK确认报文之前无法继续发送数据。如果收到之前发送的数据N字节的ACK确认报文后，如果窗口大小没有变化，则滑动窗口往右移动N个字节，因为有N个字节的数据被应答确认了，接下来又有N个字节的可用窗口了。

发送方窗口使用三个指针来跟踪四个传输类别中的每一个类别中的字节。其中两个是绝对指针（指特定的序列号），一个是相对指针（需要做偏移）：
SND.WND：表示发送窗口的大小值，由接收方指定；
SND.UNA：绝对指针，指向已经发送但未收到确认的第一个字节的序列号（发送窗口的第一个字节的序列号）；
SND.NXT：也是一个绝对指针，指向未发送但在可发送范围内的第一个字节的序列号（可用窗口的第一个字节的序列号）；
最后一个相对指针：指向未发送但超出接收方处理范围的数据的第一个字节，可以通过发送窗口的第一个字节指针加上窗口大小计算得到的偏移量。

接收方的滑动窗口：
接收方的窗口相对简单一些，根据处理的情况划分为三个部分：
已经成功接收并确认的数据
未接收到数据但可以接收的数据；
未收到数据并不可以接收的数据；
其中三个接收部分，可以使用两个指针进行划分：
RCV.WND：表示接收窗口的大小值，他会通告给发送方；
RCV.NXT：表示一个指针，指向期望从发送方发来的下一个数据字节的序列号（接收窗口的第一个字节的序列号）；
相对指针：指向未收到数据并且不可以接收的数据的首个字节的序列号，通过接口窗口的第一个字节加上窗口大小可以算出该指针位置。

接收窗口与发送窗口大小并不完全相等，接收窗口的大小是约等于发送窗口的大小的。因为滑动窗口并不是一成不变的，接收方窗口变化后，需要通过TCP报文通知发送方，这个发送过程存在一定时延，所以两者大小是约等于的关系。

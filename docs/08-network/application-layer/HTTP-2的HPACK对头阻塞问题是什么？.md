# HTTP/2的HPACK对头阻塞问题是什么？

HTTP/2的HPACK对头阻塞问题是什么？
HTTP2.0的HPACK队头阻塞
由于动态表是具有时序性的，后一个请求依赖于前一次请求双方建立的动态表，一旦前一次请求发生丢包，接收方没有成功建立动态表，后续收到的请求如果使用了动态表索引时，就无法解码出HPACK头部，后续的请求解码都会阻塞到丢失的数据包重传过来，这就是HTTP/2的HPACK队头阻塞问题。
所谓的动态表，在首次请求-响应后，双方会将未包含在静态表中的 Header 项更新各自的动态表，接着后续传输时仅用1个数字表示，然后对方可以根据这1个数字从动态表查到对应的数据，就不必每次都传输长长的数据，大大提升了编码效率。

HTTP/3的QPACK解决了这一问题，那它是如何解决的呢？
HTTP/3在头部压缩算法这方面做了升级，升级成QPACK，与HTTP/2中的HPACK编码方式相似，HTTP/3中的QPACK也采用了静态表、动态表及Huffman编码。QUIC会有两个特殊的单向流，所谓的单向流只有一端可以发送消息，双向则指两端都可以发送消息，传输HTTP消息时用的是双向流，这两个单向流的用法：
QPACK Encoder Stream：用于将一个字典(Key-Value)传递给对方，比如面对不属于静态表的HTTP 请求头部，客户端可以通过这个Stream发送字典；
QPACK Decoder Stream：用于响应对方，告诉它刚发的字典已经更新到自己的本地动态表了，后续就可以使用这个字典来编码了；
这两个特殊的单向流是用来同步双方的动态表，编码方收到解码方更新确认的通知后，才使用动态表编码HTTP 头部，从而解决了HTTP/2的HPACK队头阻塞问题。

## 面试总结
### 核心概念

HTTP/2 在一个 TCP 连接上通过二进制分帧、多路复用、Header 压缩和服务端推送提升性能。

### 面试官想考什么

面试官想考 HTTP/2 与 HTTP/1.1 的差异、HPACK、队头阻塞发生在哪一层。

### 标准回答

HTTP/2 把请求响应拆成帧并复用到同一连接，减少连接数量；HPACK 压缩重复头部；但底层仍是 TCP，一旦 TCP 丢包，同一连接上的流仍可能被阻塞，这也是 HTTP/3/QUIC 要解决的问题。

### 深挖追问

- 这个行为发生在浏览器、客户端库、代理网关还是后端服务？
- 如果接口偶发超时/失败，如何用 curl、DevTools、网关日志和 tcpdump 分层验证？
- 连接池、缓存、CDN、TLS 或反向代理配置会怎样改变现象？

### 实战场景/示例

网关到浏览器开启 HTTP/2 可降低多资源加载时的连接开销，但后端服务间是否使用仍要看协议栈和治理能力。

### 易错点/总结

不要把 HTTP/2 多路复用理解为完全没有队头阻塞，它消除的是 HTTP/1.1 应用层队头阻塞，不消除 TCP 层丢包阻塞。


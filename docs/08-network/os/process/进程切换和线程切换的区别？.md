# 进程切换和线程切换的区别？

进程切换和线程切换的区别？
进程切换
进程切换涉及到更多的内容，包括整个进程的虚拟地址空间（页表等相关内容）、全局变量、打开的文件描述符、各种寄存器（如程序计数器、通用寄存器等）、信号处理相关设置、内核栈等，进程切换的开销较大；

线程切换
线程切换只涉及到线程的栈指针（指向线程栈的位置）、通用寄存器和程序计数器等，不涉及进程级别的资源，因此线程切换的开销较小。

# 乐观锁和悲观锁的区别是什么？

乐观锁和悲观锁的区别是什么？
乐观锁：
乐观锁的基本思想是假设多个事务之间很少发生冲突，因此读取数据时不会加锁，而是在更新数据时检查数据的版本（如使用版本号或者时间戳），如果版本匹配则执行更新，否则认为发生冲突。乐观锁适合读多写少的场景，可以减少锁的竞争，提高并发性能。

悲观锁：
悲观锁的基本思想是假设多个事务之间会频繁发生冲突，因此在读取数据时会加锁，防止其他事务对数据进行修改，直到当前事务完成操作后才释放锁；悲观锁适合写多的场景，通过加锁保证数据的一致性。

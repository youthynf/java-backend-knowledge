# 段表是什么？

段表是什么？
虚拟地址除了通过页表与物理地址进行映射外，也可以通过段表与物理地址进行映射的。分段机制会把程序的虚拟地址分为4个段，每个段在段表中有一个项，在这个项找到段的物理基地址，再加上偏移量，于是就能找到物理内存中的地址。

程序的虚拟地址分为4个段，分别是：
代码段（段0）
数据段（段1）
堆段（段2）
栈段（段3）

虚拟地址由段号与段内偏移组成，而段表项则由段号、段基地址、段界限组成。

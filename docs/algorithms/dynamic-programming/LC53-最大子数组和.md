# LC53 最大子数组和

## 核心概念

最大子数组和（Maximum Subarray）要求在一个整数数组中找到一个连续子数组，使其元素和最大，并返回这个最大和。关键限制是“连续”，所以不能像子序列那样随意跳过元素。

这题是动态规划和贪心思想的经典题，也可以用分治解决。最常用解法是 Kadane 算法：维护“以当前位置结尾的最大子数组和”。

## 面试官想考什么

- 是否能定义正确的 DP 状态；
- 是否理解为什么状态只依赖前一个位置，可以空间优化到 O(1)；
- 是否能处理全负数数组；
- 是否能说清连续子数组与子序列的区别；
- 是否了解扩展问题：返回区间下标、环形最大子数组和、最大子矩阵。

## 标准回答

> 定义 `dp[i]` 为“以 `nums[i]` 结尾的连续子数组的最大和”。对于每个位置，要么把当前元素接到前一个最大子数组后面，要么从当前元素重新开始，因此转移方程是 `dp[i] = max(nums[i], dp[i-1] + nums[i])`。答案是所有 `dp[i]` 的最大值。由于 `dp[i]` 只依赖 `dp[i-1]`，可以用一个变量 `current` 滚动保存，时间复杂度 O(n)，空间复杂度 O(1)。

## 深挖追问

### 为什么答案不是最后一个 `dp[n-1]`？

因为 `dp[i]` 表示必须以第 i 个元素结尾的最大和，而全局最大子数组可能在任意位置结束，所以需要维护 `maxSum = max(maxSum, dp[i])`。

### 全负数数组怎么处理？

不能把初始最大值设为 0，否则 `[-3, -2, -5]` 会错误返回 0。应初始化为 `nums[0]`，这样答案会是最大的那个负数。

### 这题是动态规划还是贪心？

两种视角都可以。DP 视角是状态转移；贪心视角是：如果前缀和 `current` 已经小于 0，那么它只会拖累后续子数组，应从当前元素重新开始。面试中用 DP 定义更稳。

### 如何返回最大子数组的起止下标？

在决定“从当前元素重新开始”时记录候选起点，在更新全局最大值时记录最终起止位置。

## 实战场景/代码示例

### Java：只返回最大和

```java
public int maxSubArray(int[] nums) {
    int current = nums[0];
    int maxSum = nums[0];

    for (int i = 1; i < nums.length; i++) {
        current = Math.max(nums[i], current + nums[i]);
        maxSum = Math.max(maxSum, current);
    }
    return maxSum;
}
```

### Java：同时返回区间

```java
public int[] maxSubArrayWithRange(int[] nums) {
    int current = nums[0];
    int maxSum = nums[0];
    int start = 0;
    int end = 0;
    int candidateStart = 0;

    for (int i = 1; i < nums.length; i++) {
        if (current + nums[i] < nums[i]) {
            current = nums[i];
            candidateStart = i;
        } else {
            current += nums[i];
        }

        if (current > maxSum) {
            maxSum = current;
            start = candidateStart;
            end = i;
        }
    }
    return new int[]{maxSum, start, end};
}
```

### 示例推演

数组：`[-2,1,-3,4,-1,2,1,-5,4]`

| i | nums[i] | current = max(nums[i], current+nums[i]) | maxSum |
| --- | --- | --- | --- |
| 0 | -2 | -2 | -2 |
| 1 | 1 | 1 | 1 |
| 2 | -3 | -2 | 1 |
| 3 | 4 | 4 | 4 |
| 4 | -1 | 3 | 4 |
| 5 | 2 | 5 | 5 |
| 6 | 1 | 6 | 6 |
| 7 | -5 | 1 | 6 |
| 8 | 4 | 5 | 6 |

最大子数组是 `[4, -1, 2, 1]`，最大和为 6。

## 易错点/总结

- 子数组必须连续，不能跳着选；
- 全负数时答案是最大负数，不是 0；
- `dp[i]` 是以 i 结尾的局部最优，答案要取全局最大；
- 空间可优化，因为只依赖前一个状态；
- 如果数组元素和可能超过 int 范围，业务代码应使用 long；
- 返回区间时要区分候选起点和最终起点。

## 参考资料

- LeetCode 53 Maximum Subarray
- Kadane's Algorithm
- CLRS：Maximum Subarray Problem

<!-- interview-renovation:2026-06-24 -->

## 面试复习强化

### 核心概念

从面试角度看，**LC53 最大子数组和** 可以放在“算法”这一类知识中理解。复习时不要只背定义，要能说清：它解决什么问题、依赖哪些前提、正常流程是什么、异常情况下系统会怎样退化或恢复。

### 面试官想考什么

- 是否理解概念背后的设计目标，而不是只记住名词；
- 是否能把机制和真实工程场景联系起来；
- 是否能分析边界条件、失败场景、性能与安全取舍；
- 是否能给出可落地的排查、实现或优化步骤。

### 标准回答

> 说明状态/不变量、转移或选择依据、边界条件、复杂度和正确性。 追问包括空间优化、极端输入、路径还原、与相似题型转换。 对于“LC53 最大子数组和”，回答时建议先给一句话定义，再按“工作流程/关键机制 → 典型场景 → 风险与优化”展开，最后补充一两个线上实践点。

### 深挖追问

- 如果该机制失效，会出现什么现象？如何定位是配置、代码、资源还是外部依赖导致？
- 它和相邻概念有什么区别？例如语义、适用场景、性能成本、可靠性保证分别是什么？
- 在高并发、网络抖动、服务重启、数据不一致或权限受限时，需要补充哪些保护措施？
- 有哪些指标可以证明方案有效？例如延迟、吞吐、错误率、资源使用率、重试次数或业务成功率。

### 示例 / 实战场景

- 设计方案时：先明确业务目标和约束，再选择对应机制，不要为了使用某个技术而引入复杂度。
- 排查问题时：先确认现象和影响面，再查看日志、监控、配置、版本变更和上下游依赖，最后小步验证修复。
- 复盘沉淀时：补充自动化测试、容量评估、告警阈值、降级预案和文档，避免同类问题再次发生。

### 本题高频补充

- 最大子数组和可用 Kadane：dp[i] 表示以 i 结尾的最大和，转移为 max(nums[i], dp[i-1]+nums[i])。

### 易错点 / 总结

- 只背结论、不讲原因，是面试扣分点；要主动解释“为什么这样设计”。
- 只讲正常路径、不讲异常路径，会显得缺少生产经验；至少补充超时、重试、降级、回滚或兜底。
- 不要把理论保证无限放大，工程实现通常还受网络、资源、配置、版本和业务语义约束。
- 总结一句：变量含义必须清晰，不能只背代码。


# 结构型模式-适配器模式

结构型模式-适配器模式
目的是将一个类的接口转换成客户端期望的另一个接口，使不兼容的类可以一起工作。关键要点：目标接口 + 适配者 + 适配器（继承或组合适配者），实现接口转换。

// 目标接口（客户端期望的接口）
public interface TypeC {
    void chargeWithTypeC();
}

// 适配者（需要被适配的类，接口不兼容）
public class Usb {
    public void chargeWithUsb() {
        System.out.println("使用USB充电");
    }
}

// 类适配器（通过继承适配者，实现目标接口）
public class UsbToTypeCAdapter extends Usb implements TypeC {
    @Override
    public void chargeWithTypeC() {
        // 适配逻辑：将Type-C调用转换为USB调用
        super.chargeWithUsb();
        System.out.println("（通过适配器转换为Type-C）");
    }
}

// 使用示例
public class Client {
    public static void main(String[] args) {
        TypeC adapter = new UsbToTypeCAdapter();
        adapter.chargeWithTypeC();  // 输出：使用USB充电（通过适配器转换为Type-C）
    }
}

---

<!-- interview-review-enhanced -->

## 面试复习版

### 核心概念
- 结构型模式关注类/对象组合关系。
- 代理控制访问，装饰器动态增强，适配器转换接口，外观简化入口，桥接分离抽象与实现，组合表达树形结构，享元共享细粒度对象。

### 面试官想考什么
- 相似模式如何区分。
- JDK/Spring 中的实际应用。

### 标准回答
结构型模式-适配器模式 的核心是通过组合降低耦合或增强能力。面试回答先说明意图，再给出典型场景，最后与相近模式对比。

### 深挖追问
- 代理和装饰器区别？
- 适配器和外观区别？
- 组合模式如何处理叶子和容器一致性？

### 实战场景/代码示例
```java
interface Service { void call(); }
class LoggingService implements Service {
  private final Service target;
  public void call(){ System.out.println("log"); target.call(); }
}
```

### 易错点/总结
- 结构型模式常优先使用组合而非继承。
- 不要只背 UML，要能落到框架例子。

---

<!-- interview-detail-2026-06-24 -->

## 面试版详细讲解补充

### 核心概念
- 结构型模式-适配器模式 的核心是通过稳定的结构解决反复出现的设计问题，重点不在 UML 名词，而在解耦点和适用边界。
- 复习时不要只记一句结论，要把“定义、底层原因、使用边界、工程取舍”串起来。

### 面试官想考什么
- 面试官想看你能否说出模式意图、角色、优缺点，以及在真实业务中何时该用、何时不该用。
- 能否把该知识点和常见线上问题、代码设计、性能/并发/可维护性联系起来。

### 标准回答
回答 结构型模式-适配器模式 时，先讲它解决什么变化点，再讲参与角色和调用关系，最后给出 Java/Spring 中的落地例子。

如果是口述面试，建议先给一句结论，再补充 2~3 个关键细节，最后用项目场景收尾。这样既有结构，也能给面试官继续追问的抓手。

### 深挖追问
- 它和相近模式有什么区别？会不会过度设计？在 Spring 或 JDK 中哪里用到了？
- 如果让你在项目里落地这个知识点，你会如何设计测试用例验证边界？
- 遇到性能、并发或可维护性问题时，有哪些替代方案？

### 示例/实战场景
```java
// 设计模式落地时通常体现为：面向接口编程 + 组合/委派 + 隐藏变化点
```

实战中建议把该知识点放到具体场景里理解：例如接口参数校验、集合选型、线程池治理、金额计算、JVM 排障或框架扩展点，而不是孤立背概念。

### 易错点/总结
- 不要为了套模式而增加层级；模式应降低复杂度，而不是制造抽象噪音。
- 面试表达要避免绝对化，例如“永远”“一定”“只会”，很多 Java 行为都与版本、实现、参数和上下文有关。
- 最后用一句话收束：先讲清楚它解决什么问题，再讲清楚它的限制和替代方案。


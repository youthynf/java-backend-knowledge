# 结构型模式-享元模式

结构型模式-享元模式
通过共享对象减少内存占用，适用于大量相似对象（如文字处理软件中的字符）。区分内部状态（可共享，如字符 'A'）和外部状态（不可共享，如位置、颜色）。

import java.util.HashMap;
import java.util.Map;

// 享元接口
public interface Character {
    void display(int x, int y); // 外部状态：位置
}

// 具体享元：字符
public class ConcreteCharacter implements Character {
    private char c; // 内部状态（可共享）

    public ConcreteCharacter(char c) {
        this.c = c;
    }

    @Override
    public void display(int x, int y) {
        System.out.println("显示字符 '" + c + "' 在位置 (" + x + "," + y + ")");
    }
}

// 享元工厂：管理共享对象
public class CharacterFactory {
    private Map<Character, Character> characters = new HashMap<>();

    // 获取享元对象（不存在则创建）
    public Character getCharacter(char c) {
        Character character = characters.get(c);
        if (character == null) {
            character = new ConcreteCharacter(c);
            characters.put(c, character);
        }
        return character;
    }
}

// 使用示例
public class Client {
    public static void main(String[] args) {
        CharacterFactory factory = new CharacterFactory();

        // 多次获取字符'A'，实际共享同一个对象
        Character a1 = factory.getCharacter('A');
        a1.display(10, 20);

        Character a2 = factory.getCharacter('A');
        a2.display(30, 40);

        System.out.println("是否为同一对象：" + (a1 == a2)); // true
    }
}

---

<!-- interview-review-enhanced -->

## 面试复习版

### 核心概念
- 结构型模式关注类/对象组合关系。
- 代理控制访问，装饰器动态增强，适配器转换接口，外观简化入口，桥接分离抽象与实现，组合表达树形结构，享元共享细粒度对象。

### 面试官想考什么
- 相似模式如何区分。
- JDK/Spring 中的实际应用。

### 标准回答
结构型模式-享元模式 的核心是通过组合降低耦合或增强能力。面试回答先说明意图，再给出典型场景，最后与相近模式对比。

### 深挖追问
- 代理和装饰器区别？
- 适配器和外观区别？
- 组合模式如何处理叶子和容器一致性？

### 实战场景/代码示例
```java
interface Service { void call(); }
class LoggingService implements Service {
  private final Service target;
  public void call(){ System.out.println("log"); target.call(); }
}
```

### 易错点/总结
- 结构型模式常优先使用组合而非继承。
- 不要只背 UML，要能落到框架例子。


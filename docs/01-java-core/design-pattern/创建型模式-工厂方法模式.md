# 创建型模式-工厂方法模式

创建型模式-工厂方法模式
关键要点：产品接口 + 具体产品 + 工厂接口 + 具体工厂，新增产品只需扩展工厂和产品，符合开闭原则。

// 产品接口
public interface Document {
    void open();
}

// 具体产品：文本文档
public class TextDocument implements Document {
    @Override
    public void open() {
        System.out.println("打开文本文档");
    }
}

// 具体产品：表格文档
public class SpreadsheetDocument implements Document {
    @Override
    public void open() {
        System.out.println("打开表格文档");
    }
}

// 工厂接口
public interface DocumentFactory {
    Document createDocument();
}

// 具体工厂：文本文档工厂
public class TextDocumentFactory implements DocumentFactory {
    @Override
    public Document createDocument() {
        return new TextDocument();
    }
}

// 具体工厂：表格文档工厂
public class SpreadsheetDocumentFactory implements DocumentFactory {
    @Override
    public Document createDocument() {
        return new SpreadsheetDocument();
    }
}

// 使用示例
public class Client {
    public static void main(String[] args) {
        DocumentFactory factory = new TextDocumentFactory();
        Document doc = factory.createDocument();
        doc.open();  // 输出：打开文本文档
    }
}

---

<!-- interview-review-enhanced -->

## 面试复习版

### 核心概念
- 工厂模式把对象创建逻辑从使用方抽离。
- 工厂方法由子类决定创建哪种产品；抽象工厂创建一族相关产品。

### 面试官想考什么
- 简单工厂、工厂方法、抽象工厂区别。
- 如何降低 new 的耦合。

### 标准回答
当对象创建复杂或需要按条件扩展产品时，用工厂封装创建。工厂方法关注单类产品扩展，抽象工厂关注产品族一致性。

### 深挖追问
- 新增产品是否符合开闭原则？
- 工厂会不会过度设计？
- Spring BeanFactory 属于什么思想？

### 实战场景/代码示例
```java
interface ParserFactory { Parser create(); }
class JsonParserFactory implements ParserFactory { public Parser create(){ return new JsonParser(); } }
```

### 易错点/总结
- 产品很少且创建简单时无需强行工厂。
- 抽象工厂扩展产品等级结构较麻烦。

---

<!-- interview-detail-2026-06-24 -->

## 面试版详细讲解补充

### 核心概念
- 创建型模式-工厂方法模式 的核心是通过稳定的结构解决反复出现的设计问题，重点不在 UML 名词，而在解耦点和适用边界。
- 复习时不要只记一句结论，要把“定义、底层原因、使用边界、工程取舍”串起来。

### 面试官想考什么
- 面试官想看你能否说出模式意图、角色、优缺点，以及在真实业务中何时该用、何时不该用。
- 能否把该知识点和常见线上问题、代码设计、性能/并发/可维护性联系起来。

### 标准回答
回答 创建型模式-工厂方法模式 时，先讲它解决什么变化点，再讲参与角色和调用关系，最后给出 Java/Spring 中的落地例子。

如果是口述面试，建议先给一句结论，再补充 2~3 个关键细节，最后用项目场景收尾。这样既有结构，也能给面试官继续追问的抓手。

### 深挖追问
- 它和相近模式有什么区别？会不会过度设计？在 Spring 或 JDK 中哪里用到了？
- 如果让你在项目里落地这个知识点，你会如何设计测试用例验证边界？
- 遇到性能、并发或可维护性问题时，有哪些替代方案？

### 示例/实战场景
```java
// 设计模式落地时通常体现为：面向接口编程 + 组合/委派 + 隐藏变化点
```

实战中建议把该知识点放到具体场景里理解：例如接口参数校验、集合选型、线程池治理、金额计算、JVM 排障或框架扩展点，而不是孤立背概念。

### 易错点/总结
- 不要为了套模式而增加层级；模式应降低复杂度，而不是制造抽象噪音。
- 面试表达要避免绝对化，例如“永远”“一定”“只会”，很多 Java 行为都与版本、实现、参数和上下文有关。
- 最后用一句话收束：先讲清楚它解决什么问题，再讲清楚它的限制和替代方案。


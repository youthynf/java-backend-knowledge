# 行为型模式-责任链模式

行为型模式-责任链模式
将请求的发送者与接收者解耦，使多个接收者都有机会处理请求，请求沿责任链传递，直到被处理。请求需要多个对象处理（如审批流程、日志级别过滤）。

// 请求类
public class LeaveRequest {
    private int days; // 请假天数

    public LeaveRequest(int days) {
        this.days = days;
    }

    public int getDays() {
        return days;
    }
}

// 处理者抽象类
public abstract class Approver {
    protected Approver next; // 下一个处理者

    public Approver(Approver next) {
        this.next = next;
    }

    public abstract void approve(LeaveRequest request);
}

// 具体处理者：组长（处理≤3天）
public class TeamLeader extends Approver {
    public TeamLeader(Approver next) {
        super(next);
    }

    @Override
    public void approve(LeaveRequest request) {
        if (request.getDays() <= 3) {
            System.out.println("组长批准请假" + request.getDays() + "天");
        } else {
            next.approve(request); // 转发给下一级
        }
    }
}

// 具体处理者：经理（处理≤7天）
public class Manager extends Approver {
    public Manager(Approver next) {
        super(next);
    }

    @Override
    public void approve(LeaveRequest request) {
        if (request.getDays() <= 7) {
            System.out.println("经理批准请假" + request.getDays() + "天");
        } else {
            next.approve(request);
        }
    }
}

// 具体处理者：总监（处理≤30天）
public class Director extends Approver {
    public Director(Approver next) {
        super(next);
    }

    @Override
    public void approve(LeaveRequest request) {
        if (request.getDays() <= 30) {
            System.out.println("总监批准请假" + request.getDays() + "天");
        } else {
            System.out.println("请假天数过长，不批准");
        }
    }
}

// 使用示例
public class Client {
    public static void main(String[] args) {
        // 构建责任链：组长→经理→总监
        Approver chain = new TeamLeader(new Manager(new Director(null)));

        chain.approve(new LeaveRequest(2));  // 组长批准
        chain.approve(new LeaveRequest(5));  // 经理批准
        chain.approve(new LeaveRequest(20)); // 总监批准
        chain.approve(new LeaveRequest(40)); // 不批准
    }
}

---
## 核心概念
行为型模式-责任链模式 可以放在“工程实践能力”这条主线里理解。复习时不要只背结论，要先说明它解决的核心问题，再解释关键机制、适用边界和代价。围绕这个知识点，重点关注：定义、原理、边界、取舍、常见问题、排查方法和落地成本。如果面试官继续追问，通常会从“为什么这样设计、在什么场景会失效、线上如何排查”三个方向展开。

## 面试回答与追问
- **标准回答**：先给出 行为型模式-责任链模式 的定位，再说明它依赖的核心原理，最后结合业务场景说明如何使用。回答时要把“能解决什么问题”和“会带来什么成本”一起讲清楚。
- **常见追问**：如果数据量、并发量或调用链路继续放大，行为型模式-责任链模式 的瓶颈会出现在哪里？如何观测、如何优化、如何回滚？
- **易错点**：不要把概念和具体实现混在一起，也不要只说 API 名称。面试中更重要的是说清楚边界条件、失败场景和取舍依据。

## 实战场景与排查
典型落地场景包括：真实业务开发、线上问题治理、性能优化、协作交付和面试复盘。实际处理线上问题时，可以按“现象确认 → 指标采集 → 假设验证 → 小步修复 → 复盘沉淀”的路径推进。先看日志、监控、链路追踪和核心指标，再判断是容量问题、配置问题、代码路径问题，还是外部依赖抖动。

## 总结
复习 行为型模式-责任链模式 时，建议把它和相邻知识点放在一起比较：相同点是什么、区别在哪里、为什么当前场景选择它而不是替代方案。能讲清楚这些内容，才算真正掌握。

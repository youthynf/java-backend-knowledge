# 线程池中的线程是如何关闭的？

线程池中的线程是如何关闭的？
线程池中就是利用线程中断机制来停止线程的。如shutdownNow()方法中会调用：

void interruptIfStarted() {
    Thread t;
    if (getState() ≥ 0 && (t == thread) ≠ null && !t.isInterrupted()) {
        try {
            t.interrupt();
        } catch (SecurityException ignore) {
        }
    }
}

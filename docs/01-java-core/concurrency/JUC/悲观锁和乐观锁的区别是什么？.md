# 悲观锁和乐观锁的区别是什么？

悲观锁和乐观锁的区别是什么？
基本概念
•  乐观锁：
乐观锁认为竞争不总会发生，因此它不需要持有锁，将比较和替换两个动作作为一个原子操作去尝试修改内存中的变量，如果失败则表示存在竞争，那么就应该有相应的重试机制；
•  悲观锁：
悲观锁认为竞争总会发生，因此每次对资源进行操作是，都会持有一个独占的锁，就像synchronized，直接上锁操作资源。

Java中实现乐观锁的方式
CAS（Compare and Swap）
CAS是乐观锁的基础，Java提供了java.util.concurrent.atomic包，包含了各种原子变量类，这些类使用了CAS操作实现了线程安全的原子操作，可以用来实现乐观锁；
版本号控制
增加一个版本号来记录数据修改的版本，每次更新时递增版本号。在更新数据的时候同时比较版本号，只有版本号一致才更新成功，否则更新失败；
时间戳
使用时间戳记录数据的更新时间，在更新数据的时候，比较时间戳是否一致，一致则更新成功，否则更新失败；

CAS有什么缺点
可能产生ABA问题
并发环境下，一个变量在两次检查之间可能被其他线程修改并恢复为原值，导致错误的判断，Java通过AtomicStampedReference通过维护一个引用值和版本戳来实现，每次更新引用同时更新版本戳，从而是的引用的变化是可检的；
循环时间长开销大
自旋CAS方式，如果长时间不成功，则会给CPU带来很大的开销；
只能保证一个共享变量的原子操作
多个可以通过AtomicStampedReference来处理或者使用synchronized实现；

---

# 面试复习补充

## 核心概念补充

这篇文章的主题是 **悲观锁和乐观锁的区别是什么？**。复习时应先给出定义，再说明它在 Java 并发或 JVM 体系中的位置，最后结合使用场景、限制条件和常见误区展开。

## 面试官想考什么

- 是否能用自己的话讲清楚概念，而不是只背术语。
- 是否理解底层机制、关键流程以及它和相邻知识点的区别。
- 是否能把知识点落到真实项目：如何使用、如何排查、如何调优、什么时候不该用。
- 是否知道常见坑点，例如线程安全、可见性、阻塞、内存泄漏、GC 停顿或参数误用。

## 标准回答

JUC 相关问题要说明它解决的并发痛点：减少手写同步代码、提供高性能线程安全容器、原子类、锁、同步器和任务编排工具。回答时需要把 API 用法和底层机制联系起来，例如 CAS、AQS、队列、阻塞/唤醒、弱一致性迭代等。

## 深挖追问

- AQS 的 state、同步队列、独占/共享模式分别是什么作用？
- CAS 的 ABA、自旋开销、只能保护单变量等问题如何处理？
- JUC 容器和 `Collections.synchronizedXxx` 的适用场景有什么区别？
- 公平锁和非公平锁在吞吐、延迟、饥饿风险上如何取舍？

## 实战场景/代码示例

```java
AtomicInteger counter = new AtomicInteger();
int value = counter.incrementAndGet(); // 适合简单计数；复杂临界区仍应使用锁
```

## 易错点/总结

- 不要脱离场景背结论：并发和 JVM 问题通常都和负载、线程数、内存大小、JDK 版本有关。
- 面试回答建议采用“定义 → 原理 → 场景 → 风险/排查”的顺序。
- 如果涉及源码或参数，说明核心思路即可；不确定的版本差异要明确限定，不要绝对化。

---

<!-- interview-detail-2026-06-24 -->

## 面试版详细讲解补充

### 核心概念
- 悲观锁和乐观锁的区别是什么？ 属于 Java 并发基础，核心是线程安全、可见性、原子性、有序性以及 JUC 对这些问题的工程化封装。
- 复习时不要只记一句结论，要把“定义、底层原因、使用边界、工程取舍”串起来。

### 面试官想考什么
- 面试官想确认你是否理解并发问题产生的根因，而不是只会背 API 名称。
- 能否把该知识点和常见线上问题、代码设计、性能/并发/可维护性联系起来。

### 标准回答
回答 悲观锁和乐观锁的区别是什么？ 时，应先界定概念，再说明状态流转或实现机制，并结合锁、volatile、CAS、阻塞队列/线程池等组件解释实际行为。

如果是口述面试，建议先给一句结论，再补充 2~3 个关键细节，最后用项目场景收尾。这样既有结构，也能给面试官继续追问的抓手。

### 深挖追问
- 异常、取消、中断、内存可见性在该场景下如何处理？高并发下有哪些性能和死锁风险？
- 如果让你在项目里落地这个知识点，你会如何设计测试用例验证边界？
- 遇到性能、并发或可维护性问题时，有哪些替代方案？

### 示例/实战场景
```java
Future<?> f = executor.submit(task); if (!f.isDone()) f.cancel(true); // 取消依赖任务响应中断
```

实战中建议把该知识点放到具体场景里理解：例如接口参数校验、集合选型、线程池治理、金额计算、JVM 排障或框架扩展点，而不是孤立背概念。

### 易错点/总结
- 不要把线程安全等同于加 synchronized；中断不是强制杀线程；线程池参数必须结合业务压测。
- 面试表达要避免绝对化，例如“永远”“一定”“只会”，很多 Java 行为都与版本、实现、参数和上下文有关。
- 最后用一句话收束：先讲清楚它解决什么问题，再讲清楚它的限制和替代方案。


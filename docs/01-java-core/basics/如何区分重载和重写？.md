# 如何区分重载和重写？

如何区分重载和重写？
重写（Override）：
存在于继承体系中，指子类实现了一个与父类在方法声明上完全相同的一个方法。为了满足里式替换原则，重写有以下两个限制：
•  子类方法的访问权限必须大于等于父类方法；
•  子类方法的返回类型必须是父类方法返回类型或其子类型；
•  使用@Override注解，可以让编译器帮忙检查是否满足上面的两个限制条件；

重载（Overload）：
存在于同一个类中，指一个方法与已经存在的方法名称上相同，但是参数类型、个数、顺序至少有一个不同。应该注意的是，只有返回值不同不算是重载。

---

<!-- interview-review-enhanced -->

## 面试复习版

### 核心概念
- 重载是同类同名不同参数，编译期决定。
- 重写是子类重新实现父类可继承方法，运行期动态绑定。

### 面试官想考什么
- 编译期多态与运行期多态。
- 返回值不同不能构成重载。

### 标准回答
重载看参数类型、个数、顺序；重写看继承关系和方法签名，访问权限不能更窄，受检异常不能更宽。

### 深挖追问
- private/static/final 方法能重写吗？
- 构造方法能重载或重写吗？
- @Override 有什么价值？

### 实战场景/代码示例
```java
class A { void f(Number n){} }
class B extends A { @Override void f(Number n){} void f(Integer i){} }
```

### 易错点/总结
- 静态方法隐藏不是重写。
- 重载解析发生在编译期。


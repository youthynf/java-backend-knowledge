# 基本数据类型的缓存池是什么？

## 核心概念

Java 的部分包装类型内部实现了缓存池，会缓存一段常用取值范围内的对象。通过自动装箱创建包装对象时，如果值在缓存范围内，会直接复用缓存对象。

缓存池的目的主要是减少频繁创建小整数等常用对象带来的内存和性能开销。

## 常见缓存范围

| 包装类型 | 默认缓存范围 |
|---|---|
| `Byte` | -128 ~ 127 |
| `Short` | -128 ~ 127 |
| `Integer` | -128 ~ 127 |
| `Long` | -128 ~ 127 |
| `Character` | 0 ~ 127 |
| `Boolean` | `true` / `false` |

其中 `Integer` 的上限可以通过 JVM 参数调整：

```bash
-XX:AutoBoxCacheMax=<size>
```

## 示例

```java
Integer a = 127;
Integer b = 127;
System.out.println(a == b); // true

Integer c = 128;
Integer d = 128;
System.out.println(c == d); // false
```

`127` 在默认缓存范围内，所以复用同一个对象；`128` 超出默认缓存范围，自动装箱时会创建不同对象。

## 注意事项

包装类型比较数值时，不要使用 `==`，应该使用 `equals()`。

```java
Integer a = 128;
Integer b = 128;
System.out.println(a.equals(b)); // true
```

## 总结

缓存池是包装类型的对象复用优化，不应该成为业务逻辑判断的依据。面试中常用它考察自动装箱、对象引用比较和 `equals()` 的区别。

---

## 面试复习要点

面试中回答“基本数据类型的缓存池是什么”时，不要只给结论，建议按 **定位 → 原理 → 使用边界 → 排查方式** 展开：

1. **先讲定位**：说明它解决的核心问题，以及在整个技术栈中处于哪一层。
2. **再讲原理**：把关键流程、核心数据结构或关键组件讲清楚，避免只背名词。
3. **补充边界**：说明它适合什么场景，不适合什么场景，以及常见误用。
4. **结合排查**：如果线上出现性能、稳定性或一致性问题，要能说出观测指标、日志线索和排查顺序。

## 标准回答思路

可以这样组织答案：**基本数据类型的缓存池是什么 的核心价值是解决特定场景下的工程问题。实际使用时，需要理解它的工作机制、成本和限制，不能只看 API 或表面现象。在线上落地时，还要结合监控、日志和压测结果判断是否真的适合当前业务。**

## 常见追问

- 它和相近方案的区别是什么？
- 如果数据量、并发量或链路复杂度上来，会先暴露什么问题？
- 线上出现异常时，你会先看哪些指标，如何缩小范围？
